<!-- アーティスト一覧 -->
<% artists.each do |artist| %>
	<span class="profile-contents-artist-text">
		・<%= artist.like_artist %>

		<% if artist.user == current_user %>
			<%= link_to user_user_artist_path(user, artist), method: :delete, remote: true do %>
				<i class="fas fa-times-circle"></i>
			<% end %>
		<% end %>
	</span>
<% end %>