<%= form_with model: [user, artist], url: user_user_artists_path(user), remote: true do |f| %>
	<i class="fas fa-microphone-alt icon"></i><%= f.label :like_artist, '好きなアーティストを登録' %>
	<%= f.text_field :like_artist %>
	<%= f.submit '登録', class: 'btn-flat-double-border' %>
<% end %>