<%= form_with model: [user, artist], url: user_user_artists_path(user), remote: true, class:'form-artist' do |f| %>
				<%= f.label :like_artist, '好きなアーティストを登録' %>
				<%= f.text_field :like_artist %>
				<%= f.submit '登録' %>
			<% end %>